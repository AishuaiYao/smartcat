<view class="page">
  <!-- 顶部区域 -->
  <view class="top-bar">
    <text class="avatar">🐱</text>
    <text class="add-btn" bindtap="onFabTap">➕</text>
  </view>

  <!-- 标题行 -->
  <view class="title-row">
    <text class="title-left">常用</text>
    <view class="title-right-group">
      <text class="debug-btn" bindtap="onDebugTap">🔧 调试</text>
      <text class="title-right" bindtap="onSettingsTap">⚙️ 设置</text>
    </view>
  </view>

  <!-- 设备列表 -->
  <scroll-view class="device-list" scroll-y>
    <view class="card-grid">
      <view class="device-card {{showDelete ? 'show-delete' : ''}}" wx:for="{{devices}}" wx:key="id" bindtap="{{showDelete ? '' : 'onCardTap'}}" data-device="{{item}}">
        <view class="delete-btn" catchtap="onDeleteDevice" data-id="{{item.id}}">➖</view>
        <text class="card-icon">{{item.icon}}</text>
        <text class="card-name">{{item.name}}</text>
        <text class="card-status">{{item.status}}</text>
      </view>
    </view>
  </scroll-view>

  <!-- 底部导航 -->
  <view class="bottom-bar">
    <view class="nav-item" bindtap="onTabTap" data-tab="home">
      <text>🏠</text>
      <text>首页</text>
    </view>
    <view class="nav-item" bindtap="onTabTap" data-tab="smart">
      <text>🔔</text>
      <text>智能</text>
    </view>
    <view class="nav-item" bindtap="onTabTap" data-tab="assistant">
      <text>💬</text>
      <text>助手</text>
    </view>
    <view class="nav-item" bindtap="onTabTap" data-tab="my">
      <text>👤</text>
      <text>我的</text>
    </view>
  </view>

  <!-- 雷达弹窗 -->
  <view class="radar-modal {{showRadar ? 'show' : ''}}" catchtap="onRadarClose">
    <view class="radar-box" catchtap="catchTap">
      <view class="radar-area">
        <view class="radar-circle"></view>
        <view class="radar-circle c2"></view>
        <view class="radar-circle c3"></view>
        <view class="radar-sweep"></view>
        <view class="radar-center">📡</view>
      </view>
      <text class="radar-text">{{radarStatus}}</text>
      <view class="ok-btn {{showConfirm ? 'visible' : ''}}" catchtap="onConfirm">✅ 确认</view>
    </view>
  </view>
</view>
